const tabs = document.querySelectorAll(".tab");
const tabContents = document.querySelectorAll(".tab__content");

tabs.forEach(   // создаем массив и что-то с ним нужно сделать...

    tab.classList.add("tab_active");
    tabContents[index].classList.add("tab__content_active"); //выбранная вкладка, верное содержимое

)